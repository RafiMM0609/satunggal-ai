# ── Core interfaces ────────────────────────────────────────────────────────────
python-telegram-bot[webhooks]==21.9
telegramify-markdown>=0.1.0   # Konversi Markdown → Telegram MarkdownV2
fastapi==0.115.12
uvicorn[standard]==0.34.0

# ── LLM & HTTP ────────────────────────────────────────────────────────────────
httpx==0.28.1
tavily-python>=0.5.0
ollama>=0.4.0

# ── Config & validation ────────────────────────────────────────────────────────
pydantic==2.11.1
pydantic-settings==2.8.1
python-dotenv==1.0.1

# ── Excel ─────────────────────────────────────────────────────────────────────
openpyxl>=3.0.0

# ── Code search & safe patching ───────────────────────────────────────────────
scikit-learn>=1.4.0
tree-sitter>=0.22.0
tree-sitter-python>=0.23.0
tree-sitter-javascript>=0.23.0
tree-sitter-typescript>=0.23.0
patch>=1.16

# ── Optional: Semantic code search (developer agents) ─────────────────────────
# Uncomment the two lines below to enable semantic RAG ranking for
# developer_qna and developer_inspector. Requires ~80 MB model download on
# first use. Falls back to TF-IDF automatically when not installed.
# sentence-transformers>=2.2.0  # Sentence embeddings (all-MiniLM-L6-v2)
# faiss-cpu>=1.7.0              # ANN search for semantic similarity

# ── System monitoring (SysInfo Agent) ─────────────────────────────────────────
psutil>=6.0.0

# ── Document generation (Technical Writer Agent) ──────────────────────────────
weasyprint>=62.0          # Markdown → PDF via HTML+CSS
markdown>=3.7             # Markdown parser (dibutuhkan WeasyPrint pipeline)
# pandoc          → install via sistem: sudo apt install pandoc / brew install pandoc
# mermaid-cli    → install via npm:    npm install -g @mermaid-js/mermaid-cli

# ── PDF Processing (Quiz Generator) ───────────────────────────────────────────
PyMuPDF>=1.24.0           # Ekstraksi teks dari PDF (fitz)

# ── DOCX Processing (Doc Auditor Agent) ───────────────────────────────────────
python-docx>=1.1.0        # Baca & bedah file .docx (Heading → sections)

# ── Web Automation (Autonomous Browsing Agent) ─────────────────────────────────
playwright>=1.51.0         # Headless browser automation (Chromium/Firefox/WebKit)

# ── Reminder Agent (Timed Telegram Notifications) ─────────────────────────────
APScheduler>=3.10.4        # In-process async job scheduler for timed reminders
